package cn.bugstack.ai.domain.agent.service.llm.catalog;

/**
 * Feature Support Status Enum.
 *
 * <p>Distinguishes explicitly supported features from explicitly unsupported features
 * and unknown capability statuses.</p>
 */
public enum SupportStatus {
    SUPPORTED,
    UNSUPPORTED,
    UNKNOWN
}
