export interface EvalDataset { id:string; key:string; name:string; description:string; version:number; enabled:boolean; baselineRunId:string; caseCount:number; createdAt:number; updatedAt:number; cases?:EvalCase[]; runs?:EvalRun[] }
export interface EvalCase { id:string; datasetId:string; caseKey:string; name:string; agentId:string; prompt:string; expectations:Record<string,unknown>; rubric:Record<string,unknown>; tags:string[]; enabled:boolean; version:number }
export interface EvalRun { id:string; datasetId:string; candidateLabel:string; status:string; baselineRunId:string; gateStatus:string; errorMessage:string; repeatCount:number; totalCases:number; completedCases:number; passedCases:number; failedCases:number; regressionCount:number; averageScore:number; averageDurationMs:number; totalInputTokens:number; totalOutputTokens:number; summary:Record<string,unknown>; createdAt:number; startedAt:number; completedAt:number; caseRuns?:EvalCaseRun[] }
export interface EvalAssertion { id:string; graderType:string; key:string; description:string; passed:boolean; hardGate:boolean; score:number; maxScore:number; expected:unknown; actual:unknown; details:string }
export interface EvalCaseRun { id:string; evalRunId:string; caseId:string; invocationId:string; sessionId:string; status:string; caseKey:string; caseName:string; agentId:string; errorMessage:string; repeatIndex:number; caseVersion:number; finalOutput:string; totalScore:number; passed:boolean; durationMs:number; inputTokens:number; outputTokens:number; toolCalls:number; modelCalls:number; scoreBreakdown:Record<string,number>; assertions:EvalAssertion[] }
